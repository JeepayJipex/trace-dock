# ===========================================
# TRACE-DOCK Server Environment Variables
# ===========================================

# Server port (default: 3001)
PORT=3001

# ===========================================
# Database Configuration
# ===========================================

# Database type: sqlite | postgresql | mysql
# Default: sqlite
DB_TYPE=sqlite

# Database connection URL
# For SQLite: file path (default: ./data/trace-dock.sqlite)
# For PostgreSQL: postgres://user:password@host:port/database
# For MySQL: mysql://user:password@host:port/database
# DATABASE_URL=./data/trace-dock.sqlite

# Legacy SQLite-specific paths (backwards compatible)
# DATA_DIR=./data
# DB_PATH=./data/trace-dock.sqlite

# Enable database debug logging
# DB_DEBUG=false

# ===========================================
# CORS Configuration
# ===========================================

# Comma-separated list of allowed origins
# Example: "https://app.example.com,https://admin.example.com"
# If not set, defaults to localhost origins for development
CORS_ORIGINS=http://localhost:5173,http://localhost:3001,http://127.0.0.1:5173

# Set to "true" to allow all origins (use with caution in production!)
# CORS_ALLOW_ALL=true

# ===========================================
# Database Examples
# ===========================================

# SQLite (default - no additional setup needed)
# DB_TYPE=sqlite
# DATABASE_URL=./data/trace-dock.sqlite

# PostgreSQL
# DB_TYPE=postgresql
# DATABASE_URL=postgres://postgres:password@localhost:5432/tracedock

# MySQL / MariaDB
# DB_TYPE=mysql
# DATABASE_URL=mysql://root:password@localhost:3306/tracedock